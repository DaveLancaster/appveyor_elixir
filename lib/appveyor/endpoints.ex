defmodule Appveyor.Endpoints do
  def collaborators do 
    "/collaborators"
  end

  def environments do
    "/environments"
  end

  def projects do
    "/projects"
  end

  def users do
    "/users"
  end

  def roles do
    "/roles"
  end
end
